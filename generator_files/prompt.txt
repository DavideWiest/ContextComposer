Split up following pieces of information into markdown files.
Each concept/module/part should have its own file.
Ignore irrelevant information.
if you want to create a file or append to an existing one, write "NEWFILE: " + the file name.
Then write its contents.
Make sure to only return the files.

Information:
{text}
